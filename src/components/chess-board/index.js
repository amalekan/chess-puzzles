/*jshint esversion: 6 */
const controller = require('./chess-board.controller');
const template = require('./chess-board.html');

const component = {
  controller,
  template
};

module.exports = component;
